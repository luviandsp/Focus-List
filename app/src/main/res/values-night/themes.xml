<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FocusList" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="fontFamily">@font/roboto</item>
    </style>

    <style name="Theme.FocusList.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_grey</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_icon_foreground</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.FocusList</item>
    </style>

    <style name="Theme.FocusList" parent="Base.Theme.FocusList" />
</resources>